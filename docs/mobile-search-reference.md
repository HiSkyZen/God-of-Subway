# Mobile search layout intent

The departure and arrival inputs are field cards: the small `출발역` / `도착역` label sits inside the same rounded border as the large station value. On mobile the cards stack vertically with a centered circular swap affordance between them. The time-adjustment row remains a single six-cell row and stretches to the full available width without a trailing empty strip.
